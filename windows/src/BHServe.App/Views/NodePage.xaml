<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BHServe.App.Views.NodePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Node.js" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock Opacity="0.7" Text="Multiple Node versions via fnm (downloaded on first use)"/>
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <TextBox x:Name="VerBox" PlaceholderText="version (e.g. 20, 22, lts)" Width="220"/>
                <Button Content="Install" Click="Install_Click" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="Set default" Click="Default_Click"/>
                <Button Content="Uninstall" Click="Uninstall_Click"/>
                <ProgressRing x:Name="Busy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" BorderThickness="1" CornerRadius="8" Padding="14"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ScrollViewer>
                <TextBlock x:Name="OutputBox" FontFamily="Consolas" TextWrapping="Wrap"
                           Text="Installed versions appear here."/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
