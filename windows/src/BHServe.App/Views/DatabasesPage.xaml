<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BHServe.App.Views.DatabasesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BHServe.App.Views">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Databases" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock x:Name="ServerStatus" Opacity="0.7" Text="MySQL/MariaDB on 127.0.0.1:3306 (root · no password)"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button x:Name="StartDbBtn" Content="Start server" Click="StartDb_Click" Style="{StaticResource AccentButtonStyle}"/>
            <Button x:Name="StopDbBtn"  Content="Stop server"  Click="StopDb_Click"/>
            <ProgressRing x:Name="Busy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <TextBox x:Name="NameBox" PlaceholderText="new database name" Width="220"/>
                <TextBox x:Name="PassBox" PlaceholderText="user password (optional)" Width="200"/>
                <Button Content="Create" Click="Create_Click" Style="{StaticResource AccentButtonStyle}"/>
                <TextBlock Text="blank = root/no-password" Opacity="0.6" VerticalAlignment="Center" FontSize="12"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ListView x:Name="DbList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid Padding="8,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind}" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="Drop" Tag="{x:Bind}" Click="Drop_Click"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="10">
            <TextBlock Text="DB tools:" VerticalAlignment="Center" Opacity="0.7"/>
            <Button Content="phpMyAdmin" Click="Pma_Click"/>
            <Button Content="Adminer"    Click="Adminer_Click"/>
            <Button Content="Mailpit"    Click="Mailpit_Click"/>
        </StackPanel>
    </Grid>
</Page>
