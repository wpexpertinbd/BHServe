<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BHServe.App.Views.DatabasesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BHServe.App.Views">

    <ScrollViewer>
    <StackPanel Padding="24" Spacing="16">
        <TextBlock Text="Databases" FontSize="26" FontWeight="SemiBold"/>

        <!-- servers -->
        <Border BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Spacing="10">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock x:Name="DbEngineLabel" Text="MySQL / MariaDB" Width="140" VerticalAlignment="Center" Opacity="0.7"/>
                    <Button x:Name="StartDbBtn" Content="Start" Click="StartDb_Click" Style="{StaticResource AccentButtonStyle}"/>
                    <Button x:Name="StopDbBtn"  Content="Stop"  Click="StopDb_Click"/>
                    <Button Content="Root password…" Click="RootPw_Click"/>
                    <TextBlock x:Name="RootPwStatus" VerticalAlignment="Center" Opacity="0.7" FontSize="12"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <TextBlock Text="PostgreSQL" Width="120" VerticalAlignment="Center" Opacity="0.7"/>
                    <Button x:Name="StartPgBtn" Content="Start" Click="StartPg_Click"/>
                    <Button x:Name="StopPgBtn"  Content="Stop"  Click="StopPg_Click"/>
                    <Button x:Name="InstallPgBtn" Content="Install PostgreSQL" Click="InstallPg_Click"/>
                    <TextBlock x:Name="PgStatus" VerticalAlignment="Center" Opacity="0.7" FontSize="12"/>
                    <ProgressRing x:Name="Busy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- create -->
        <Border BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <ComboBox x:Name="EngineBox" Width="140" PlaceholderText="no engine running" SelectionChanged="Engine_Changed"/>
                <TextBox x:Name="NameBox" PlaceholderText="new database name" Width="200"/>
                <TextBox x:Name="PassBox" PlaceholderText="user password (MySQL, optional)" Width="200"/>
                <Button x:Name="GenBtn" Content="Generate" Click="GenPw_Click"/>
                <Button x:Name="CreateBtn" Content="Create" Click="Create_Click" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- list -->
        <Border BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel>
                <Grid Padding="8,6" ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                        <TextBlock Text="Show" VerticalAlignment="Center" Opacity="0.7"/>
                        <ComboBox x:Name="DbPageSizeBox" Width="80" SelectionChanged="DbPageSize_Changed"/>
                    </StackPanel>
                    <TextBox Grid.Column="2" x:Name="DbSearchBox" PlaceholderText="search…" Width="220" TextChanged="DbSearch_Changed"/>
                </Grid>
                <TextBlock x:Name="EmptyDbs" Text="No databases yet." Opacity="0.6" Padding="10" Visibility="Collapsed"/>
                <ItemsControl x:Name="DbList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="local:DbRow">
                            <Grid Padding="8,6" ColumnSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{x:Bind Name}" VerticalAlignment="Center"/>
                                <Border Grid.Column="1" VerticalAlignment="Center" CornerRadius="4" Padding="7,2" Margin="0,0,6,0"
                                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                    <TextBlock Text="{x:Bind Engine}" FontSize="11" Opacity="0.8"/>
                                </Border>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6">
                                    <Button Content="Password…" Tag="{x:Bind Name}" Click="DbPassword_Click" Visibility="{x:Bind MysqlVis}"/>
                                    <Button Content="Drop" Tag="{x:Bind Key}" Click="Drop_Click"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Grid x:Name="DbPager" Padding="8,8" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Button x:Name="DbPrevBtn" Click="DbPrev_Click"><FontIcon Glyph="&#xE76B;" FontSize="12"/></Button>
                        <TextBlock x:Name="DbPageLabel" VerticalAlignment="Center" Opacity="0.8"/>
                        <Button x:Name="DbNextBtn" Click="DbNext_Click"><FontIcon Glyph="&#xE76C;" FontSize="12"/></Button>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="DB tools:" VerticalAlignment="Center" Opacity="0.7"/>
            <Button Content="phpMyAdmin" Click="Pma_Click"/>
            <Button Content="Adminer"    Click="Adminer_Click"/>
            <Button Content="Mailpit"    Click="Mailpit_Click"/>
        </StackPanel>
    </StackPanel>
    </ScrollViewer>
</Page>
