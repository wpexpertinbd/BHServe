<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BHServe.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BHServe.App.Views">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12">
            <StackPanel Spacing="2">
                <TextBlock Text="Dashboard" FontSize="26" FontWeight="SemiBold"/>
                <TextBlock x:Name="SubTitle" Opacity="0.7"
                           Text="Local web stack — nginx + php-cgi over TCP"/>
            </StackPanel>
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" CornerRadius="8" Padding="12" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="CPU" Opacity="0.6" FontSize="12"/>
                        <TextBlock x:Name="CpuText" FontSize="20" FontWeight="SemiBold" Text="—"/>
                        <ProgressBar x:Name="CpuBar" Minimum="0" Maximum="100"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" CornerRadius="8" Padding="12" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Memory" Opacity="0.6" FontSize="12"/>
                        <TextBlock x:Name="MemText" FontSize="20" FontWeight="SemiBold" Text="—"/>
                        <ProgressBar x:Name="MemBar" Minimum="0" Maximum="100"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" CornerRadius="8" Padding="12" BorderThickness="1"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <StackPanel Spacing="4">
                        <TextBlock Text="Disk (system)" Opacity="0.6" FontSize="12"/>
                        <TextBlock x:Name="DiskText" FontSize="20" FontWeight="SemiBold" Text="—"/>
                        <ProgressBar x:Name="DiskBar" Minimum="0" Maximum="100"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <Button x:Name="StartBtn"   Click="StartAll_Click"   Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE768;" FontSize="14"/><TextBlock Text="Start all"/>
                </StackPanel>
            </Button>
            <Button x:Name="StopBtn"    Click="StopAll_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE71A;" FontSize="14"/><TextBlock Text="Stop all"/>
                </StackPanel>
            </Button>
            <Button x:Name="RestartBtn" Click="RestartAll_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/><TextBlock Text="Restart"/>
                </StackPanel>
            </Button>
            <ProgressRing x:Name="Busy" IsActive="False" Width="20" Height="20" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="4">
            <ListView x:Name="ServicesList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ServiceRow">
                        <Grid Padding="8,6" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse Grid.Column="0" Width="10" Height="10" Fill="{x:Bind DotBrush}"/>
                            <TextBlock Grid.Column="1" Text="{x:Bind Key}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{x:Bind StatusText}" Opacity="0.7" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{x:Bind AutoText}" Opacity="0.6" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Expander Grid.Row="3" Header="Activity log" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
            <ScrollViewer x:Name="LogScroll" Height="120">
                <TextBlock x:Name="LogBox" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Expander>
    </Grid>
</Page>
