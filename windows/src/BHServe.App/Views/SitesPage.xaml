<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BHServe.App.Views.SitesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BHServe.App.Views">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sites" FontSize="26" FontWeight="SemiBold"/>

        <Border Grid.Row="1" BorderThickness="1" CornerRadius="8" Padding="12"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                <TextBox x:Name="NameBox" PlaceholderText="site name (e.g. myapp)" Width="200"/>
                <ComboBox x:Name="PhpBox" Width="110" SelectedIndex="0"/>
                <ComboBox x:Name="TypeBox" Width="140" SelectedIndex="0">
                    <ComboBoxItem Content="others"/>
                    <ComboBoxItem Content="php"/>
                    <ComboBoxItem Content="wordpress"/>
                </ComboBox>
                <Button x:Name="AddBtn" Content="Add site" Click="Add_Click" Style="{StaticResource AccentButtonStyle}"/>
                <ProgressRing x:Name="Busy" IsActive="False" Width="20" Height="20"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" BorderThickness="1" CornerRadius="8" Padding="4"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ListView x:Name="SitesList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:SiteRow">
                        <Grid Padding="8,6" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind Name}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <HyperlinkButton Grid.Column="1" Content="{x:Bind Url}" NavigateUri="{x:Bind Uri}" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{x:Bind Php}" Opacity="0.7" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" Spacing="6">
                                <Button Content="Secure" Tag="{x:Bind Domain}" Click="Secure_Click" IsEnabled="{x:Bind NotSecure}"/>
                                <Button Content="Remove" Tag="{x:Bind Name}" Click="Remove_Click"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
